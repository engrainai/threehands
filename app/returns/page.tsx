export const metadata = {
  title: "Returns | Three Hands Vintage"
};

export default function ReturnsPage() {
  return (
    <main className="content-page">
      <section className="page-hero">
        <p className="kicker">Returns</p>
        <h1>Returns</h1>
      </section>
      <section className="content-shell">
        <p>Vintage watches include a 3-day inspection period.</p>
        <p>
          Please contact us before returning a watch. Approved returns may be refunded less a 15% restocking
          fee, or credited for the full value of the watch excluding shipping.
        </p>
        <p>Returned watches must arrive in the same condition in which they were received.</p>
      </section>
    </main>
  );
}
